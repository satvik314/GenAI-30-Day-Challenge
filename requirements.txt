# GenAI 30-Day Challenge - Python Dependencies

# Core AI/ML Libraries
openai>=1.12.0
anthropic>=0.18.0
python-dotenv>=1.0.0

# LangChain Ecosystem (Days 7, 19, 20, 27)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20

# Image Generation (Days 12, 14)
diffusers>=0.25.0
transformers>=4.36.0
accelerate>=0.25.0
torch>=2.1.0
torchvision>=0.16.0
Pillow>=10.0.0

# Vector Databases & Embeddings (Days 15, 16, 18)
chromadb>=0.4.22
faiss-cpu>=1.7.4
sentence-transformers>=2.3.0

# Audio Processing (Days 22, 23)
openai-whisper>=20231117
pydub>=0.25.1

# Web & API Development (Day 30)
fastapi>=0.109.0
uvicorn>=0.27.0
streamlit>=1.30.0
gradio>=4.16.0

# Utilities
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0
python-multipart>=0.0.6

# Development & Testing
pytest>=7.4.0
black>=23.12.0
flake8>=7.0.0

# Optional - For specific advanced use cases
# tiktoken>=0.5.2  # Token counting
# pinecone-client>=3.0.0  # Pinecone vector DB
# weaviate-client>=4.4.0  # Weaviate vector DB
# ollama>=0.1.0  # Ollama Python SDK (Day 26)

# Note: Some packages may require additional system dependencies
# - For audio (Whisper): ffmpeg
# - For GPU support: CUDA toolkit and cuDNN
# - For local LLMs (Ollama): Install from https://ollama.ai

# Installation Instructions:
# 1. Create virtual environment: python -m venv venv
# 2. Activate: source venv/bin/activate (Linux/Mac) or venv\Scripts\activate (Windows)
# 3. Install: pip install -r requirements.txt
# 4. For GPU support on CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
